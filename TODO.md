# TODO: Make User's Stars Glow Prominently

## Steps
- [x] Track user's own stars via localStorage (`emotionGalaxy_myStars`)
- [x] Create enhanced star texture for user's stars (white-hot core, larger glow, extra spikes)
- [x] Modify `buildStars()` to use different texture + bigger scale for user stars
- [x] Modify `addMessage()` to save user's star IDs
- [x] Modify `animate()` for enhanced pulsing/float/opacity on user stars
- [x] Load user star IDs on startup
- [x] ✅ Complete

